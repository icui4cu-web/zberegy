---
import "./input.scss";
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"input"> {
	class?: string;
	size?: "xs";
}

const { class: className, size, ...rest } = Astro.props;
---

<input class:list={[className, "input", size && `input_size_${size}`]} {...rest} />
