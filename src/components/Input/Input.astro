---
import "./input.scss";
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<"input"> {
	class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<input class:list={[className, "input"]} {...rest} />
