---
import "./icon.scss";

interface Props {
	id: string;
	class?: string;
	width?: string;
	height?: string;
}

const { class: className, id, width, height } = Astro.props;
---

<svg class:list={[className, "icon"]} {width} {height}>
	<use href={`/assets/img/icons.svg#icon-${id}`}></use>
</svg>
