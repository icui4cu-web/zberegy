---
import Icon from "@components/Icon/Icon.astro";
import "./socials.scss";

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<ul class:list={[className, "socials"]}>
	{
		[
			{
				title: "Facebook",
				icon: "facebook-circle",
			},
			{
				title: "Youtube",
				icon: "youtube-circle",
			},
			{
				title: "Twitter",
				icon: "twitter-circle",
			},
		].map(({ title, icon }) => (
			<li class="socials__item">
				<a class="socials__link" href="#" {title} target="_blank" rel="nofollow">
					<Icon id={icon} />
				</a>
			</li>
		))
	}
</ul>
